apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "customer-workflow-helm.fullname" . }}-test-connection"
  labels:
    {{- include "customer-workflow-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "customer-workflow-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
